<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java.demo.mapper.BoardMapper">
    <select id="selectAll" resultType="com.java.demo.dto.BoardDto">
        select * from board
        <!--<if test="category == 'all'">
            where btitle like '%'||#{word}||'%' or name like '%'||#{word}||'%'
        </if>
        <if test="category == 'btitle'">
            where btitle like '%'||#{word}||'%'
        </if>
        <if test="category == 'name'">
            where name like '%'||#{word}||'%'
        </if>-->
    </select>

    <select id="selectOne" resultType="com.java.demo.dto.BoardDto">
        select * from board where bno=#{bno}
    </select>

</mapper>